# PCSSAK AroDamA 보안 정책

- 적용 버전: 0.4.x 무료 Early Access
- 작성일: 2026-08-30

이 문서는 취약점 신고와 출시 보안 기준을 설명하며 법률 자문이나 보안 인증을 뜻하지 않습니다.

## 지원 범위

| 버전 | 상태 | 보안 수정 |
|---|---|---|
| 0.4.x | 무료 Early Access | 최신 공개 0.4.x에 합리적으로 가능한 범위에서 보안 수정 제공 |
| 0.3.x | 이전 내부 QA | 스키마 v6→v7 전환 검증용, 신규 보안 수정 없음 |
| 0.2.x | 이전 내부 QA | 스키마 v5→v6 전환 검증용, 신규 보안 수정 없음 |
| 0.1.x 이하 | 실험 빌드 | 지원하지 않음 |

Early Access 지원 범위나 종료 기준이 바뀌면 공식 지원 페이지와 이 표를 함께 갱신합니다.

## 취약점 신고

공개 이슈나 게시판에 취약점 세부 정보, 클립보드 내용, 데이터베이스, 암호화된 원문,
Windows 사용자 이름, 파일 경로, 인증 코드 또는 비밀번호를 올리지 마십시오.

공식 공개 배포 저장소의 GitHub **Private vulnerability reporting**을 사용하십시오.

`https://github.com/pcssakinc/pcssak-arodama-releases/security/advisories/new`

일반 제품 지원은 `support@pcssak.com`으로 문의할 수 있지만, 공개되기 전의 취약점 세부
정보와 사용자 데이터는 이메일이나 공개 Issue로 보내지 말고 위 비공개 신고 경로를
사용하십시오. 신고에는 다음 최소 정보만 포함하십시오.

1. 영향받는 버전과 Windows 버전·아키텍처
2. 재현 단계와 예상 결과·실제 결과
3. 개인정보를 제거한 로그 또는 화면
4. 권한 상승, 네트워크, 사용자 상호작용 필요 여부
5. 가능한 영향과 임시 완화 방법

## 우선 점검 대상

- 클립보드 데이터의 평문 저장·로그 노출·권한 경계 우회
- DPAPI 암복호화 오류, HMAC 중복 판별키 손상·교체, 손상 데이터 처리, 키 범위 오해
- AroDamA 삭제 기록 보관함의 무료 24시간 복구 경계 우회, 24시간 이후 복구, 30일 만료 기록 노출 또는 프런트엔드 조작에 의한 복구 권한 상승
- 기억 사건의 시각·결과 변조, 대상 앱 이용행태 노출, 완료되지 않은 작업의 거짓 성공 표시
- 파일 경로·HTML·RTF·등록 이미지·OLE 가상 파일·OCR 입력의 메모리 안전성, COM 수명과 자원 고갈
- 휴대용 백업의 KDF 자원 상한·인증 실패 구분 노출·경로 재분석 지점·기존 파일 원자 교체·복원 중 전원 차단과 자동 롤백
- 전역 단축키와 `SendInput` 대상 창 오인으로 인한 의도하지 않은 붙여넣기
- Tauri 명령 권한·CSP 우회와 임의 명령 실행
- Tauri 업데이터 키 노출·분실, 서명 검증 우회, 설치·업데이트 파일 변조, 공식 배포본과 다른 파일, 다운그레이드·롤백 공격
- 로그·지원 자료에 포함된 클립보드 내용과 개인 경로

## 출시 보안 기준

- 최소 권한 Tauri capability와 제한된 CSP를 유지하고 새 권한은 위협 모델 검토 후 추가합니다.
- 잠금 파일 기반으로 npm·Cargo 의존성을 재현하고 취약점·라이선스를 릴리스마다 검사합니다.
- x64와 x86 산출물을 동일한 소스·버전에서 만들고 각각 테스트합니다.
- 코드 서명 구현은 이번 무료 Early Access 후보 범위에서 제외합니다. 현재 후보가 Authenticode 미서명임과 Windows가 `알 수 없는 게시자` 또는 SmartScreen 경고를 표시할 수 있음을 다운로드 전에 알립니다.
- Tauri 업데이트 무결성 서명은 Authenticode와 별개로 유지합니다. 개인키는 소스·공개 저장소·로그에 넣지 않고 현재 사용자 보호 보관소와 분리된 오프라인 복구본으로 관리하며, 공개키만 앱에 내장합니다.
- PCssak가 통제하는 공식 배포 경로에 각 파일의 제품 버전·아키텍처·파일 크기·SHA-256을 함께 게시하고 깨끗한 VM 설치·악성코드·백신 오탐 검사를 수행합니다. 해시는 파일 동일성을 확인할 뿐 게시자 신원이나 안전성을 보증하지 않습니다.
- 진단 로그는 기본적으로 클립보드 본문을 기록하지 않고, 내보내기 전에 사용자가 내용을 확인·삭제할 수 있어야 합니다.

## 로컬 데이터 무결성과 키 복구

- 활성 기록과 AroDamA 삭제 기록 보관함의 본문·미리보기·HTML·RTF·이미지·실제/가상 파일 목록, 자주 쓰는 문구 본문, OCR 결과, 태그·보드 이름과 썸네일은 현재 Windows 사용자 범위의 DPAPI로 보호합니다. 현재 항목의 출처 앱, 자주 쓰는 문구 이름, 기억 사건의 식별자·종류·시작/완료 시각·결과·횟수·출처 구분·붙여넣기 방식, 삭제·무료 복구·만료 시각, 태그·보드 소속과 붙여넣기 순서 같은 운영 메타데이터는 평문일 수 있습니다. 사건 안의 복사 출처 또는 선택적으로 저장한 붙여넣기 대상 실행 파일 이름은 DPAPI로 보호합니다.
- 콘텐츠 중복 판별값은 설치별 비밀키를 사용하는 HMAC-SHA-256입니다. 키는 `dedup-key.bin`에 DPAPI로 보호해 저장하며, HMAC은 콘텐츠 암호화를 대신하지 않습니다.
- 전체 본문 검색은 별도 설치별 키의 HMAC 후보 필터와 후보별 정확 복호화 검증을 사용합니다. 실행 경로에 평문 본문 RAM 인덱스를 유지하지 않고, 필터가 없거나 손상된 행은 누락시키지 않도록 전체 후보 검증으로 낮춥니다. 이 설계는 검색어·접근 패턴·메모리 공격까지 숨기는 검색 가능 암호화 체계를 뜻하지 않습니다.
- `dedup-key.bin`이 비어 있거나 변조·손상되면 원본 파일을 `corrupt` 표식 이름으로 보존하고 새 키를 원자적으로 만든 뒤 중복 판별값을 다시 계산합니다. 본문 DPAPI 복호화까지 실패하면 일부 데이터로 조용히 진행하지 않고 마이그레이션을 롤백해 시작을 중단합니다.
- 기존 DB는 쓰기 전에 SQLite `quick_check`를 통과해야 합니다. 스키마 또는 HMAC 전환 전에는 DB 내부에 무작위 시도 표식을 먼저 동기화하고 `VACUUM INTO` 일관 복구본의 SHA-256과 정확한 파일명을 연결합니다. `pending → ready → cleanup` 상태 전이는 엄격한 WAL 반영 뒤 진행하므로 전환이나 정리 중 종료돼도 다음 시작에서 이어갑니다. 현재 DB의 `quick_check`, 현재 스키마 필수 열·표와 HMAC 키 표식이 확인된 경우에만 복구본을 삭제합니다. 현재 DB가 완성됐지만 복구본 손상·잠금 때문에 정리만 실패하면 경고 후 재시도하며, 표식과 연결되지 않은 파일은 자동 삭제하지 않습니다.
- 사용자가 직접 삭제한 기록은 활성 표에서 AroDamA 전용 삭제 기록 보관 표로 같은 트랜잭션 안에서 이동하고 안정적인 `memory_id`로 연결된 기억 사건도 같은 수명주기를 따릅니다. 삭제 시각을 기준으로 무료 24시간 복구와 전체 30일 보관·만료 경계를 백엔드에서 판정합니다. 24시간이 지난 콘텐츠와 기억 사건도 향후 기능 기반을 위해 같은 Windows 사용자 문맥에서 최대 30일까지 로컬 암호화 보관하지만 현재 후보에서는 복구할 수 없습니다. 삭제 기록 보관함과 기억 사건도 512MB 논리 저장량 한도에 포함합니다.
- 24시간 이후 복구는 프런트엔드 조작으로 승인되어서는 안 됩니다. 현재 후보에는 Pro·결제·판매·원격 라이선스 활성화 기능이나 관련 UI가 없으며, 백엔드는 24시간이 지난 항목의 복구를 거부합니다.
- 영구 삭제는 언제나 허용하고, 정확히 30일이 된 만료 항목은 다음 정리 시 앱 DB에서 제거합니다. 다만 SQLite 페이지·WAL·백업 매체까지 덮어쓰는 물리적 보안 삭제나 복구 불가능성을 보장하지 않습니다.
- `붙여넣기 대상 앱 기억`은 기본값을 꺼짐으로 유지합니다. 켜도 실행 파일 이름만 저장하고 문서·창 제목, 전체 경로, PID와 HWND는 저장하지 않습니다. 다시 끄면 이후 대상부터 기록하지 않으며 기존 값은 자동으로 바꾸지 않습니다. 설정의 `저장된 대상 앱 이름 삭제`는 두 단계 확인 뒤 과거 붙여넣기 사건의 `app_enc`만 현재 앱 DB에서 영구 `NULL` 처리하고 사건 자체는 보존합니다. 복사 출처 앱 이름에는 이 삭제를 적용하지 않습니다.
- 사용자 제외 앱과 일부 대표 비밀번호 관리자의 정확한 실행 파일 basename은 클립보드 payload를 읽기 전에 판정하고, 같은 정책을 이후 붙여넣기 대상 앱 이름 원장에도 적용합니다. 선택형 민감 텍스트 검사는 기본적으로 끄고, 사용자가 켠 경우에만 개인 키·인식 가능한 토큰·일반 결제 카드 형식과 Luhn 검사를 통과한 번호를 로컬에서 좁게 차단합니다. 이 필터들은 보안 경계가 아니며 기존 기록을 자동 검사·삭제하지 않습니다.
- 복사나 자동 붙여넣기를 시작하기 전에 `pending` 사건을 먼저 커밋하고, 완료 결과와 기존 누계 갱신을 같은 트랜잭션으로 처리합니다. 앱 시작 시 남은 `pending`은 `interrupted`로 바꿉니다. 사건 완료 기록에 실패했는데 외부 동작 결과가 있을 수 있으면 성공이 아니라 불확실로 알립니다.
- `sent`는 Windows `SendInput`이 Ctrl+V 입력을 입력 스트림에 모두 삽입했다는 뜻일 뿐, 대상 앱이 내용을 수신·표시·저장했다는 확인이 아닙니다. AroDamA 밖에서 사용자가 직접 실행한 Ctrl+V는 관찰하거나 기록하지 않습니다.
- 스키마 v5에는 사건별 과거 기록이 없으므로 v6은 당시 마지막 복사 시각의 원장 시작 요약과 기존 마지막 자동 붙여넣기 시각의 누계 요약만 만듭니다. 과거의 개별 시각·앱·붙여넣기 방식·결과를 추정하지 않습니다.
- 기억 사건과 무료 24시간 복구·전체 30일 보관 경계는 로컬 Windows 시스템 시각을 사용합니다. 시각 변경은 표시와 복구·만료 판정에 영향을 줄 수 있고 DB 메타데이터에는 변조 방지 서명이나 신뢰 시각원이 없습니다. 따라서 이 기능을 감사 로그나 법적 증거로 사용하지 않습니다.
- 휴대용 백업은 SQLite 온라인 스냅샷의 논리 레코드를 현재 DPAPI 문맥에서 풀어 Argon2id와 XChaCha20-Poly1305로 다시 보호합니다. 내보내기는 같은 볼륨 임시 파일을 끝까지 인증 검증한 뒤에만 게시하고, 가져오기는 격리 후보의 형식·상한·DB `quick_check`·외래키·스키마 계약을 통과한 경우에만 재시작 교체를 준비합니다. 교체 뒤 첫 부팅을 확정하지 못하면 검증된 원본으로 자동 롤백합니다. 앱 데이터 경로·재분석 지점·하드링크를 백업 대상으로 사용하지 못하게 해야 하며, 백업 암호 분실과 같은 사용자 권한 악성코드·스왑·크래시 덤프까지 방어한다고 표현하지 않습니다.
- 삭제 기록 보관함과 휴대용 백업은 별개입니다. 분실한 로컬 DB, 백업 파일 또는 백업 암호를 AroDamA가 대신 복구할 수 없습니다.
- 클립보드 리스너 큐는 유한 크기이며 초과·메시지 루프 종료·작업 스레드 시작 실패를 지속 장애 상태로 기록합니다. 이 상태에서는 거짓 재개를 거부하고 앱 재시작을 안내합니다.
- 단일 인스턴스 뮤텍스와 활성화 이벤트는 Tauri와 DB를 열기 전에 확보합니다. 새 버전을 시작할 때 이미 실행 중인 0.2.1은 구버전 뮤텍스와 숨은 창 메시지 계약으로 활성화하고, 현재 세대끼리의 중복 실행은 DB 전에 차단합니다. 더 새 스키마를 연 뒤 이전 실행 파일을 직접 시작하는 다운그레이드 방향은 이전 버전이 미래 스키마를 안전하게 거부하고 종료하는 방식이며 양방향 데이터 호환으로 보증하지 않습니다. 종료 이벤트로 대기 스레드를 회수한 뒤에만 Win32 핸들을 닫습니다.

공식 참고 자료: [Tauri 보안 capability](https://v2.tauri.app/security/capabilities/),
[Tauri CSP](https://v2.tauri.app/security/csp/),
[Microsoft SmartScreen 평판](https://learn.microsoft.com/windows/apps/package-and-deploy/smartscreen-reputation),
[Microsoft SendInput](https://learn.microsoft.com/windows/win32/api/winuser/nf-winuser-sendinput).

## 보안 한계

로컬 저장과 DPAPI는 완전한 비밀 보장을 의미하지 않습니다. 이미 사용자 세션을 장악한
악성 프로그램, 사용자가 허용한 원격 제어, 화면·키 입력·메모리 가로채기 또는 물리적
접근까지 이 앱만으로 차단할 수 없습니다. 보안 관련 마케팅은 검증된 구현 범위를 넘어선
절대 표현을 사용하지 않습니다.
